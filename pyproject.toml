[project]
name = "remote-post-it"
version = "0.1.0"
description = "A web-connected TODO receipt printer"
requires-python = ">=3.13"
dependencies = [
    "pyboxen>=1.3.0",
    "python-dotenv>=1.0,<2",
]
